# Generated by `cargo-aidoc`. Its lines run to 169 columns and `just aidoc`
# writes them again on every regeneration; a formatter here would fight the
# generator every time.
docs/aidoc/

# Bodies written to be posted, not committed. GitHub folds a paragraph
# itself, so these must not be wrapped — the opposite of what this config
# does — and the directory is gitignored anyway.
workspace/

target/
node_modules/
crates/asterism-ui/dist/
crates/asterism-ui/src-tauri/gen/
