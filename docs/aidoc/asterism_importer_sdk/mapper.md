# asterism-importer-sdk::mapper

Convenience shape for parser output plus the mapping to the wire
`AddAssetCommand`.

## Functions

- `spec_to_command` — Turns an `AssetSpec` into the wire command.

## Types

- `AssetSpec` — What a parser hands back to the pipeline.

