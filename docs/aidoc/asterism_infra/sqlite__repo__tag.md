# asterism-infra::sqlite::repo::tag

SQLite adapter for the `TagRepository` port (backed by rusqlite-isle).

## Types

- `SqliteTagRepository` — SQLite adapter for `TagRepository` (uses a writer isle).

