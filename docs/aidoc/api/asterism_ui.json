{
  "crate": "asterism-ui",
  "version": "0.0.0",
  "items": [
    {
      "path": "asterism_ui::commands",
      "kind": "module",
      "docs": "Tauri command handlers — a thin translation layer. They pass DTOs\nthrough to the application services in `asterism-core` and convert\n`DomainError` into `UiError`. No business logic lives here.\n\nEvery mutation that writes to *this machine* names its attribution\nchannel explicitly — [`AttributionContext::owner_surface`]. This is\nthe owner's own operation surface (the desktop app's IPC), so the\nowner-ness is a property of the surface rather than a guess about the\ncaller, and the commands carry no attribution fields for it to read.\nThe argument is required by the service signatures, so a new\nmutation cannot be added here without choosing.\n\nThe qualifier is #153's and covers exactly one block, at the end of\nthis file: the verbs that write to a **team**. There the author is\nthe authenticated member and the team's server stamps it, so a\ncontext stated here would be a second answer to a settled question.\nTwo of those verbs — connecting and publishing — write nothing\nthrough a service that takes a context, and `publish_line_to_team`\nadditionally writes local relation rows, which carry no actor at\nall. The block says all of this where it sits.\n\n# This surface and the HTTP one are mirrors\n\nNot \"mostly overlapping\" — a verb on one belongs on the other, in the\nsame change. `asterism-server`'s `http` module doc states the rule\nand the two differences that are by design: attribution, and where\nthe id comes from.\n\nMCP is not part of that obligation. It is curated on purpose, which\nits own module doc explains.\n\n# Why they are mirrors and not one path\n\nThis process serves that HTTP surface itself — `lib.rs` spawns the\nloopback listener over `asterism_server::http::router`, and\n`state.rs` builds its context from the same `CoreCtx` these commands\nhold. So a command *could* answer by calling it, and the service\ncall each verb carries would then be written once rather than on\nboth sides.\n\nIt does not, and the reason is what the second path buys back. A\ncommand reaching the loopback surface would serialise its arguments,\ncross a socket, and deserialise a response the process already holds\nin memory, on every call the window makes — including the ones a\nscroll makes tens of at a time. Commands take the contract's types\nand call the application services directly instead.\n\nWhat that costs is the thing the rule above exists to bound: two\ntransports over one service graph, either of which can gain a verb\nthe other does not. The obligation is that a person can do the same\nwork over HTTP — not that both take the same route to the service,\nwhich they deliberately do not. `asterism-server`'s\n`tests/transport_parity.rs` is what holds the two lists to each\nother."
    },
    {
      "path": "asterism_ui::commands::accept_tag_suggestion",
      "kind": "function",
      "docs": "Accepts one tag suggestion (#112): the ruling lands on the\nevidence row and the tag is linked in `asset_tag`."
    },
    {
      "path": "asterism_ui::commands::active_profile",
      "kind": "function",
      "docs": "Returns the active local data profile for persistent UI chrome."
    },
    {
      "path": "asterism_ui::commands::add_asset",
      "kind": "function",
      "docs": "Ingests an asset (entry point for the asset-add pipeline)."
    },
    {
      "path": "asterism_ui::commands::add_asset_batch",
      "kind": "function",
      "docs": "Ingests a batch of assets (bulk form of `add_asset`)."
    },
    {
      "path": "asterism_ui::commands::add_asset_to_group",
      "kind": "function",
      "docs": "Idempotent add of an asset to a Group."
    },
    {
      "path": "asterism_ui::commands::amend_forge_message",
      "kind": "function",
      "docs": "Corrects something said.\n\n**Answers with the correction, not with the message as it now\nreads.** The model keeps both and the distinction is the model's:\nwhat was said first is still there, and an answer shaped as \"the\nmessage, updated\" would quietly be the shape that loses it."
    },
    {
      "path": "asterism_ui::commands::append_thread_message",
      "kind": "function",
      "docs": "Appends one Message. UI-side callers pass `author_kind = \"human\"`."
    },
    {
      "path": "asterism_ui::commands::archive_forge_line",
      "kind": "function",
      "docs": "Finished with. Takes no landing until it is reopened, and is the\nonly state a drop can be reached from."
    },
    {
      "path": "asterism_ui::commands::archive_persona",
      "kind": "function",
      "docs": "Toggles a persona's archive flag."
    },
    {
      "path": "asterism_ui::commands::archive_thread",
      "kind": "function",
      "docs": "Toggles the archived flag."
    },
    {
      "path": "asterism_ui::commands::asset_constellation",
      "kind": "function",
      "docs": "Returns the fully-resolved hover-burst payload — each edge with\nthe card it lands on, already filtered by viewer visibility."
    },
    {
      "path": "asterism_ui::commands::asset_declare_meta",
      "kind": "function",
      "docs": "Records — or removes — one AlbumMeta statement on an asset: the\ncommand twin of `POST /asterism/assets/{id}/album-meta`.\n\nThe statement's own `operator_ai` travels on the command and is a\ndifferent subject from who the asset is by, which is why the\nattribution handed to the service is still the owner surface. Saying\nsomething about a row is not authoring it."
    },
    {
      "path": "asterism_ui::commands::asset_declare_provenance",
      "kind": "function",
      "docs": "Declares (or repairs) an asset's origin after the fact — the\ncommand twin of `POST /asterism/assets/{id}/provenance`. Same\n`derived_from` vocabulary as ingest; an unresolvable claim is\nrecorded on `extra._trace`, not rejected."
    },
    {
      "path": "asterism_ui::commands::asset_declare_source_type",
      "kind": "function",
      "docs": "Asserts — or retracts, via an absent `source_type` — the asset's\ndigital source type by hand: the command twin of\n`POST /asterism/assets/{id}/source-type`. A term the IPTC\nvocabulary does not define is refused at the door.\n\nLike [`asset_declare_meta`], the statement's own `operator_ai`\ntravels on the command and is a different subject from who the\nasset is by, which is why the attribution handed to the service is\nstill the owner surface."
    },
    {
      "path": "asterism_ui::commands::asset_detail",
      "kind": "function",
      "docs": "Detail view (asset + attached tags + constellation edges)."
    },
    {
      "path": "asterism_ui::commands::asset_edges",
      "kind": "function",
      "docs": "Returns the top-`limit` edges (by weight) for hover-burst\nrendering."
    },
    {
      "path": "asterism_ui::commands::asset_lineage",
      "kind": "function",
      "docs": "Walks the whole `derived_from` chain around the asset, not just\nits immediate neighbours — the read side of correlation ingest.\n\n`depth` is hops in each direction (clamped server-side); the\nresponse says whether it had to stop early, and carries the\ndispatch ids the chain passed through as its backbone."
    },
    {
      "path": "asterism_ui::commands::asset_provenance",
      "kind": "function",
      "docs": "Returns the 1-hop `derived_from` lineage around the asset —\nancestors (Selection inputs this asset was derived from) and\ndescendants (assets later derived from this one). Powers the\ndetail-pane Provenance section."
    },
    {
      "path": "asterism_ui::commands::asset_source_type",
      "kind": "function",
      "docs": "What the asset's source type currently rests on — the read twin of\n`GET /asterism/assets/{id}/source-type`: the container's evidence\nand the person's assertion, each on its own, with \"not yet\nfingerprinted\" kept distinct from \"declares nothing\"."
    },
    {
      "path": "asterism_ui::commands::asset_texts",
      "kind": "function",
      "docs": "Resolves the full source text of each asset (session Reader\nview). Unreadable sources come back as `text = None` so the UI\ncan fall back to the stored cover snippet."
    },
    {
      "path": "asterism_ui::commands::asset_video_preview",
      "kind": "function",
      "docs": "Where a video's transcoded preview rendition stands — the command\ntwin of `GET /asterism/assets/{id}/video-preview`. The first call\nfor a missing rendition enqueues the transcode; the pane polls\nwhile `pending`."
    },
    {
      "path": "asterism_ui::commands::attach_tag",
      "kind": "function",
      "docs": "Attaches a tag to an asset by name (creates the tag row on first\nuse). Idempotent on both the row and the m:n link."
    },
    {
      "path": "asterism_ui::commands::attach_tag_batch",
      "kind": "function",
      "docs": "Attaches one tag to many assets in one call (grid multi-select)."
    },
    {
      "path": "asterism_ui::commands::batch_group_membership",
      "kind": "function",
      "docs": "Bulk attach / detach of asset↔group pairs. Returns\n`[attached, detached]` written-row counts."
    },
    {
      "path": "asterism_ui::commands::clone_shared_entry",
      "kind": "function",
      "docs": "Takes a copy of one entry of a shared line (#148 decision 10).\n\nAn import, and it lands through the same door every other import\nlands through — which is why the answer is an ordinary `AssetDto`\nand why the duplicate machinery recognises the second ask."
    },
    {
      "path": "asterism_ui::commands::close_forge_pursuit",
      "kind": "function",
      "docs": "Ends the work, and puts what it says on the line if it says\nanything.\n\n**A [`UiError::Conflict`] from here is not always worth retrying,\nand its `reason` says which kind it is:** `\"blocked\"` wants\nsomething done first and the message says what, `\"raced\"` will\nusually win on a retry, `\"settled\"` never helps, and `\"clashes\"` has\nto ask for something different. They are spelled out on\n`close_forge_pursuit` in `asterism-server`'s `http` module,\nincluding why `\"blocked\"` arrives for two different reasons."
    },
    {
      "path": "asterism_ui::commands::close_shared_pursuit",
      "kind": "function",
      "docs": "Ends work on a shared line, landing it or abandoning it.\n\n`outcome` is the forge's word — a satisfied close is the only\nmoment anything a round asked for reaches the line."
    },
    {
      "path": "asterism_ui::commands::connect_team_server",
      "kind": "function",
      "docs": "Logs this window in to a team server and holds the session.\n\n`remember` is the mint: with it, the session this login opened is\nused once more to ask the server for a device token, which goes in\nthis machine's keychain so the next window reconnects without a\npassword. What that stores and where is\n[`stored_connection`](crate::stored_connection); the password is\nnot part of it under any key.\n\nBeing remembered is not a condition of connecting. A mint that\nfails leaves a window logged in and a box that will have to be\nticked again, which is the honest outcome — refusing the connection\nover it would take away the thing that did work.\n\nThat is why the session is installed **before** the mint is\nattempted rather than after it, and why the mint's refusal does not\nleave this function: a locked keychain, a denied prompt and a\nserver that would not mint all end here as a warning in the diag\nlog and a connected window. Nothing tells the person, which is a\nreal cost and the smaller one — the box is unticked next time they\nlook, and a modal saying a credential was not saved is a modal\nabout something they can simply do again. Saying so on the return\nshape is the alternative, and it puts a field across the wire, the\nbindings and the store to carry a fact nothing acts on."
    },
    {
      "path": "asterism_ui::commands::connect_team_server_stored",
      "kind": "function",
      "docs": "Reconnects from the device token this machine holds, without asking\nanybody anything.\n\nThe silent half of #204: a restart reaches its team from the\nkeychain, and the password form is what a rejection falls back to.\nWhat lands in [`AppState::teams`](crate::state::AppState::teams) is\nan ordinary session — the same one\n[`connect_team_server`] hands over — so nothing downstream knows\nwhich arm was used.\n\nA `401` forgets both halves before answering. Keeping a token the\nserver has said no to buys a second silent failure on the next\nlaunch and a listing row nobody can match to a machine.\n\nWhich of the three ends this reached, and why none of them is an\nerror, is on [`StoredTeamConnectOutcome`]."
    },
    {
      "path": "asterism_ui::commands::create_dir",
      "kind": "function",
      "docs": "Creates a Dir under the given persona."
    },
    {
      "path": "asterism_ui::commands::create_dispatch",
      "kind": "function",
      "docs": "Kicks off one exporter run against a Selection. The apalis\n`DispatchRun` job picks it up on the next tick; the UI polls\n[`get_dispatch`] until the state is terminal."
    },
    {
      "path": "asterism_ui::commands::create_group",
      "kind": "function",
      "docs": "Creates a Group under the given persona."
    },
    {
      "path": "asterism_ui::commands::create_material_layer",
      "kind": "function",
      "docs": "Opens a band the person owns. Never the default — see\n[`SetDefaultMaterialLayerCommand`] for moving that flag."
    },
    {
      "path": "asterism_ui::commands::create_modality",
      "kind": "function",
      "docs": "Registers a new modality master row."
    },
    {
      "path": "asterism_ui::commands::create_query_group",
      "kind": "function",
      "docs": "\"Save as Group\": mints a `kind='query'` Group from a `query_json`\nv1 rule and evaluates it synchronously once."
    },
    {
      "path": "asterism_ui::commands::create_series_strategy",
      "kind": "function",
      "docs": "Registers a series rule and asks for the keys it implies — the\ncommand twin of `POST /asterism/series-strategies`. A rule this\nbuild could not carry out is refused before the row is written."
    },
    {
      "path": "asterism_ui::commands::create_snapshot",
      "kind": "function",
      "docs": "Freezes a picked asset list into a Snapshot and stops there\n(content-hash deduped).\n\nThe freeze half of `promote_volatile_selection` without the promote.\nBoth other Snapshot-consuming commands (`create_dispatch`,\n`get_snapshot`) take a snapshot id, and until this existed the only\nway to obtain one was to also create a Group — so \"keep this exact\nset for now\" had to go through a side effect nobody asked for."
    },
    {
      "path": "asterism_ui::commands::create_team",
      "kind": "function",
      "docs": "Founds a team on the connected server, owned by the signed-in\naccount.\n\nAnswers with the id alone. The wire also returns the ledger event\nthe creation appended, and a screen reads that where every other\nact of the team's is read rather than out of a write's response."
    },
    {
      "path": "asterism_ui::commands::create_thread",
      "kind": "function",
      "docs": "Creates a Thread."
    },
    {
      "path": "asterism_ui::commands::delete_asset_comment",
      "kind": "function",
      "docs": "Deletes a comment. Idempotent."
    },
    {
      "path": "asterism_ui::commands::delete_chapter_mark",
      "kind": "function",
      "docs": "Removes one section. **Not** idempotent, unlike deleting a mark: a\nchapter is named by `(layer_id, chapter_id)`, so an id that is not in\nthat band is a refusal rather than a no-op."
    },
    {
      "path": "asterism_ui::commands::delete_dir",
      "kind": "function",
      "docs": "Deletes an **empty** Dir."
    },
    {
      "path": "asterism_ui::commands::delete_material_layer",
      "kind": "function",
      "docs": "Deletes a band the person owns, with everything in it. Refuses an\nimported or machine band."
    },
    {
      "path": "asterism_ui::commands::delete_material_mark",
      "kind": "function",
      "docs": "Deletes a mark. Idempotent."
    },
    {
      "path": "asterism_ui::commands::delete_modality",
      "kind": "function",
      "docs": "Deletes a modality master row — only when no asset carries the slug\n(`409 Conflict` otherwise; hide it instead)."
    },
    {
      "path": "asterism_ui::commands::delete_persona_profile",
      "kind": "function",
      "docs": "Removes the persona profile row entirely."
    },
    {
      "path": "asterism_ui::commands::delete_persona_theme",
      "kind": "function",
      "docs": "Removes the persona theme row entirely (reverts to defaults)."
    },
    {
      "path": "asterism_ui::commands::delete_series_strategy",
      "kind": "function",
      "docs": "Removes a series rule and, by the schema's cascade, every key\nderived under it — the command twin of\n`DELETE /asterism/series-strategies/{id}`. No guard, unlike\n[`delete_modality`]: a series key is recomputed from rows already\nin hand."
    },
    {
      "path": "asterism_ui::commands::delete_session",
      "kind": "function",
      "docs": "Deletes a Session — only when no `asset` row still references\nit (`409 Conflict` otherwise). Mirror of the Modality delete\nguard; the UI disables the tile's ✕ button when\n`message_count > 0` so this error only surfaces under a race."
    },
    {
      "path": "asterism_ui::commands::delete_tag",
      "kind": "function",
      "docs": "Drops a tag channel and every link to it — the command twin of\n`POST /asterism/tags/delete`. Unlike [`detach_tag`], this removes\nthe channel itself; there is no trash for tags."
    },
    {
      "path": "asterism_ui::commands::delete_thread",
      "kind": "function",
      "docs": "Deletes a Thread (cascades to messages)."
    },
    {
      "path": "asterism_ui::commands::delete_thread_message",
      "kind": "function",
      "docs": "Deletes one Message (misfire correction)."
    },
    {
      "path": "asterism_ui::commands::detach_tag",
      "kind": "function",
      "docs": "Removes a tag from an asset. Idempotent — a missing link is a\nno-op; the tag row is left in place for other assets."
    },
    {
      "path": "asterism_ui::commands::detach_tag_batch",
      "kind": "function",
      "docs": "Detaches one tag from many assets in one call."
    },
    {
      "path": "asterism_ui::commands::discard_forge_line",
      "kind": "function",
      "docs": "Takes the line, its history and every piece of work against it.\n\n**The answer is the point.** It names the assets the drop released,\nand after this write there is no record left to derive them from, so\na caller that ignores it has lost the only answer there will be."
    },
    {
      "path": "asterism_ui::commands::disconnect_team_server",
      "kind": "function",
      "docs": "Drops the session, and drops what this machine was keeping.\n\n**Signing out revokes the device token; closing the window does\nnot.** The issue leaves this open and this is the answer: pressing\ndisconnect is somebody saying they are done here, and a stored\ncredential outliving that would make the button a lie on every\nmachine but the server's listing. A window closing says nothing of\nthe kind — nothing in this process runs on the way out, which is\nwhat makes \"close the window\" the gesture that keeps the\nconnection.\n\nThe revoke is best effort in the same sense the logout already is:\na server that cannot be reached to be told must not be able to hold\nsomebody in a session, so the local half happens either way. What\nthat costs is a row left in the owner's listing when the server was\ndown, revocable from the panel the next time it answers, and the\ntoken behind it is gone from this machine regardless.\n\n**What is revoked and forgotten is the connection being ended, not\nwhatever the file happens to name.** The two come apart on a path\nthat is reached rather than theoretical — a remembered server that\nwas down leaves its metadata in place and its login in the connect\nform, which is editable over — and a verb that followed the file\nthere would send one server's handle to another and wipe the first\none's entry while its row stayed live. So the stored half is\ntouched only when it names this connection, and otherwise this is a\nlogout and nothing more:\n[`stored_connection`](crate::stored_connection)'s \"which connection\na verb acts on\" is where that rule is stated for all three verbs\nthat ask it."
    },
    {
      "path": "asterism_ui::commands::dispatch_run",
      "kind": "function",
      "docs": "Live-source dispatch (`dispatch_run`): freezes a Group (query\ngroups are refreshed first) or a volatile selection into a deduped\nSnapshot, stamps the provenance, and enqueues the run."
    },
    {
      "path": "asterism_ui::commands::edit_asset_comment",
      "kind": "function",
      "docs": "Rewrites the body of an existing comment (stamps `edited_at`)."
    },
    {
      "path": "asterism_ui::commands::edit_chapter_mark",
      "kind": "function",
      "docs": "Retitles a section and, unlike the mark face, may move it: the reason\na person keeps a band of their own is usually that the file's\ndivisions are in the wrong places."
    },
    {
      "path": "asterism_ui::commands::edit_material_mark",
      "kind": "function",
      "docs": "Rewrites the body of an existing mark (stamps `edited_at`). The\nanchor is left where it is — this verb rewords a mark, it does not\nmove one."
    },
    {
      "path": "asterism_ui::commands::empty_trash",
      "kind": "function",
      "docs": "Permanently deletes every asset in the trash. Irreversible, and\ndeliberately unfiltered — see `EmptyTrashCommand`."
    },
    {
      "path": "asterism_ui::commands::get_asset_thumb",
      "kind": "function",
      "docs": "Returns the cached JPEG bytes of a thumbnail for `asset_id` at\n`size_px`, or `None` when the pair is not cached yet (`cover_gen`\nor the importer has not written one). The Svelte side wraps the\nbytes in a `Blob` and hands the URL to `<img src>`."
    },
    {
      "path": "asterism_ui::commands::get_asset_thumbs",
      "kind": "function",
      "docs": "The same thing for a whole screenful: cached JPEG bytes for each of\n`asset_ids` at `size_px`, **in the order asked**, `None` where the\npair is not cached yet.\n\nThe grid paints tens of tiles per scroll, and asking per tile made\nthat tens of IPC round trips per scroll — measured at 8,263 single\nfetches across 1,000 jumps, with p95 reaching 10.4 s once the blob\ncache stopped absorbing the repeats [measured 2026-08-05,\nbench-scroll-v3]. One call per screenful is the shape the caller\nactually has.\n\nMisses still enqueue `thumb_gen`, one job per missing pair — the\njobs are per asset by nature, so batching the read does not batch\nthose."
    },
    {
      "path": "asterism_ui::commands::get_dispatch",
      "kind": "function",
      "docs": "Fetches a dispatch job by id — used by the poll loop that drives\nthe progress badge."
    },
    {
      "path": "asterism_ui::commands::get_forge_line",
      "kind": "function",
      "docs": "The line and its whole history.\n\nThis grows with the line. A surface showing what is *on* a line\nwants [`get_forge_line_states`] instead; this one is for showing how\nit got there."
    },
    {
      "path": "asterism_ui::commands::get_forge_line_states",
      "kind": "function",
      "docs": "What is on the line, folded from the chain."
    },
    {
      "path": "asterism_ui::commands::get_forge_pursuit",
      "kind": "function",
      "docs": "The work, whole — one read rather than the line's two."
    },
    {
      "path": "asterism_ui::commands::get_forge_pursuit_behind",
      "kind": "function",
      "docs": "The landings this work has not seen, oldest first.\n\nHow far behind rather than what collides: a landing here may touch\nnothing this work asks for. A screen reads both — this one to say\nthe line has moved, [`get_forge_pursuit_collisions`] to say whether\nit matters."
    },
    {
      "path": "asterism_ui::commands::get_forge_pursuit_collisions",
      "kind": "function",
      "docs": "What this work still asks for that the line has moved since.\n\nDerived from both logs on every call, so it cannot go stale. This is\nwhat a screen shows before offering [`resolve_forge_pursuit`]."
    },
    {
      "path": "asterism_ui::commands::get_forge_thread",
      "kind": "function",
      "docs": "The conversation, whole — every message and every correction to\neach.\n\nA payload carrying only what each message says now would leave a\nwithdrawn sentence attributed to the person who withdrew it."
    },
    {
      "path": "asterism_ui::commands::get_persona_profile",
      "kind": "function",
      "docs": "Fetches the persona's identity signal (avatar / bio / role).\n`None` means no profile row is set."
    },
    {
      "path": "asterism_ui::commands::get_persona_theme",
      "kind": "function",
      "docs": "Fetches the persona's UI chrome (wallpaper reference). `None`\nmeans no custom theme is set."
    },
    {
      "path": "asterism_ui::commands::get_session",
      "kind": "function",
      "docs": "Fetches one Session by surrogate id (`None` when absent). Used\nby SessionsView after an inline edit to re-hydrate the tile\nwithout a full `list_sessions` round-trip. Contract:\n[`SessionService::get`](asterism_core::application::SessionService::get)."
    },
    {
      "path": "asterism_ui::commands::get_snapshot",
      "kind": "function",
      "docs": "Snapshot view metadata (`snapshot_get`): the freeze's id,\ncontent hash, and frozen member ids."
    },
    {
      "path": "asterism_ui::commands::get_thread",
      "kind": "function",
      "docs": "Fetches one Thread by id."
    },
    {
      "path": "asterism_ui::commands::groups_of_asset",
      "kind": "function",
      "docs": "Which Groups the asset already sits in — powers the \"already\nadded\" state on the detail-overlay dropdown."
    },
    {
      "path": "asterism_ui::commands::head_status",
      "kind": "function",
      "docs": "Which trained head scores tags, and what a next training run would\nlearn from — the command twin of `GET /asterism/heads/status`\n(#130)."
    },
    {
      "path": "asterism_ui::commands::hydrate_cards",
      "kind": "function",
      "docs": "Batch-hydrates cards by id. Companion to `list_asset_index` —\nthe frontend calls this for the ~40 rows the VList is about\nto paint plus a small prefetch window. Ids that don't exist\nor are hidden from the viewer drop out of the response."
    },
    {
      "path": "asterism_ui::commands::jobs_stats",
      "kind": "function",
      "docs": "Snapshot of the apalis `Jobs` table used by the UI progress\nbanner. Polled every few seconds by the front-end. Mapped to the\ncontract wire form so the shape reaches `bindings.ts` through\nschema-bridge (the infra type stays off the contract layer)."
    },
    {
      "path": "asterism_ui::commands::link_group",
      "kind": "function",
      "docs": "Connects a Group into another Group (cycle- / persona-guarded)."
    },
    {
      "path": "asterism_ui::commands::list_asset_comments",
      "kind": "function",
      "docs": "Lists every comment on `asset_id` in chronological order."
    },
    {
      "path": "asterism_ui::commands::list_asset_index",
      "kind": "function",
      "docs": "Index-only grid listing for 6-figure result sets. Returns\n`AssetIndex` projections — no cover text / source locator /\nfile size — so the frontend can eager-load the full ordering\nand virtualised scroll without paying the card-serialisation\ncost. The visible viewport is hydrated separately via\n`hydrate_cards`."
    },
    {
      "path": "asterism_ui::commands::list_assets",
      "kind": "function",
      "docs": "Lists assets for the grid (returns `AssetCard` projections)."
    },
    {
      "path": "asterism_ui::commands::list_chapter_marks",
      "kind": "function",
      "docs": "Lists the sections in one band, in the reading order the band states\n(which need not be the timeline's). What a panel re-reads after\nediting a single band."
    },
    {
      "path": "asterism_ui::commands::list_color_asset_counts",
      "kind": "function",
      "docs": "Sidebar COLOR facet counts — `(bucket, count)` per palette swatch\n(`red` / `blue` / `white` / …) carried by a top-level asset, in\nswatch order, optionally persona-scoped. Swatches nothing carries\nare omitted."
    },
    {
      "path": "asterism_ui::commands::list_dirs",
      "kind": "function",
      "docs": "Sidebar Dir tree (flat `parent_id` list; the UI assembles it)."
    },
    {
      "path": "asterism_ui::commands::list_dispatch",
      "kind": "function",
      "docs": "Lists dispatch jobs with the same predicate surface as the HTTP\nlist endpoint — the UI renders a per-persona history panel."
    },
    {
      "path": "asterism_ui::commands::list_duplicate_conflicts",
      "kind": "function",
      "docs": "The duplicate questions still waiting on a person, newest first,\nboth sides hydrated as cards.\n\nDistinct from `list_duplicate_groups`, which keeps reporting a pair\nthat has been ruled to be two separate things: this is the panel's\nwork list."
    },
    {
      "path": "asterism_ui::commands::list_duplicate_groups",
      "kind": "function",
      "docs": "Duplicate report — sets of live assets sharing a fingerprint on\n`axis` (`\"artefact\"` by default, `\"content\"` for the bytes that\ndecide the decoded result), optionally persona-scoped.\n\nThe counts ride along so the panel never has to present an empty\nreport as a clean bill of health: `unhashed_count` is \"still\nlooking\", `unreadable_count` is \"these originals could not be read\nwhen the pass tried, and the number will not move until the files\ncome back\", `unwalked_count` is \"the content axis has no reading of\nthese files, because the migration that fills the column in could not\nopen their originals\"."
    },
    {
      "path": "asterism_ui::commands::list_events",
      "kind": "function",
      "docs": "Newest-first telemetry listing (kind / time-window filters). Feeds\nlocal usage summaries; the HTTP twin serves agent-side aggregation."
    },
    {
      "path": "asterism_ui::commands::list_exporters",
      "kind": "function",
      "docs": "Registered exporter slugs — the action bar renders one row per\nentry. Empty vector means the current build shipped no exporters."
    },
    {
      "path": "asterism_ui::commands::list_forge_lines",
      "kind": "function",
      "docs": "Every line, without its history."
    },
    {
      "path": "asterism_ui::commands::list_forge_pursuit_children",
      "kind": "function",
      "docs": "Work opened from this work."
    },
    {
      "path": "asterism_ui::commands::list_forge_pursuits_of_line",
      "kind": "function",
      "docs": "Every piece of work against a line, open and ended alike."
    },
    {
      "path": "asterism_ui::commands::list_forge_strategies",
      "kind": "function",
      "docs": "Every rule a line can be pointed at, built from the rules this\ndeployment carries."
    },
    {
      "path": "asterism_ui::commands::list_forge_threads_about",
      "kind": "function",
      "docs": "Conversations about one thing in the forge — the work as a whole,\none round of it, one entry as that round had it, or what landed on a\nline.\n\n**One command where HTTP has four routes.** There are four there\nbecause a path cannot express a wrong combination of ids, and that\nproperty is the route's rather than the question's: IPC has no path,\nso the combination has to be checked wherever it arrives. It is\nchecked here, by the same resolver [`open_forge_thread`] names its\nanchor through — which is the shape the forge already uses when\nthere is no path to lean on, and the shape [`list_threads`] uses for\nthe same job on the app-level Threads. `anchor_kind` says which ids\nare wanted: `\"pursuit\"` wants `pursuit_id`, `\"round\"` adds\n`node_id`, `\"entry\"` adds `entry_id` to that, and `\"change\"` wants\n`line_id` with `change_point_id`.\n\nBoth directions are refused, and the second is the one that matters:\na missing id is obvious, while an id the kind has no use for would\notherwise be ignored — `\"round\"` carrying an `entry_id` would answer\nabout the round for a caller that asked about the entry, which is\nexactly what the four routes exist to make impossible.\n\nMore than one conversation can hang off the same thing — two people\nstarting separate ones about a round is not a mistake to merge — so\nthis answers a list."
    },
    {
      "path": "asterism_ui::commands::list_format_asset_counts",
      "kind": "function",
      "docs": "Sidebar FORMAT facet counts (asset-model v4) — `(format, count)`\nper mime top-level type (`image` / `video` / `audio` / `text`) on\ntop-level assets' primary materials, optionally persona-scoped."
    },
    {
      "path": "asterism_ui::commands::list_group_links",
      "kind": "function",
      "docs": "Every Group-in-Group connection in scope — the UI builds the\nnesting graph (child bands, descendant filter expansion) from it."
    },
    {
      "path": "asterism_ui::commands::list_groups",
      "kind": "function",
      "docs": "Sidebar Groups section."
    },
    {
      "path": "asterism_ui::commands::list_material_layers",
      "kind": "function",
      "docs": "Lists every band over `asset_id`'s material, each with the chapters\nin it, in display order.\n\nOne call rather than one per band: the panel needs the bands to choose\nbetween and the contents of the chosen one at the same moment. An\nannotation band's `chapters` is always empty — those hold notes, read\nthrough `list_material_marks`."
    },
    {
      "path": "asterism_ui::commands::list_material_marks",
      "kind": "function",
      "docs": "Lists every mark in `asset_id`'s material, in the material's own\norder (`start_ms` ascending, ties broken by id)."
    },
    {
      "path": "asterism_ui::commands::list_modalities",
      "kind": "function",
      "docs": "Modality master listing — one row per registered modality (hidden\nincluded), ordered by `sort_order` then `slug`, each carrying its\nlive asset count. Drives the sidebar axis + `slug → kind` catalog."
    },
    {
      "path": "asterism_ui::commands::list_modality_asset_counts",
      "kind": "function",
      "docs": "Sidebar Modality counts — `(modality_slug, asset_count)`, optionally\nscoped to one persona (`None` = cross-persona total)."
    },
    {
      "path": "asterism_ui::commands::list_observations",
      "kind": "function",
      "docs": "Every observation stream on one timeline, newest first — the\ncommand twin of `GET /asterism/observations`. Carries the shared\nenvelope only; a stream's own columns stay with that stream's own\nsocket endpoint (diagnostics the desktop does not read)."
    },
    {
      "path": "asterism_ui::commands::list_persona_asset_counts",
      "kind": "function",
      "docs": "Sidebar Persona counts — `(persona_id, asset_count)` per persona\nthat owns at least one asset. Ordered by count DESC then uuid ASC."
    },
    {
      "path": "asterism_ui::commands::list_personas",
      "kind": "function",
      "docs": "Lists every persona (used to render the sidebar)."
    },
    {
      "path": "asterism_ui::commands::list_series_strategies",
      "kind": "function",
      "docs": "Every registered series rule, oldest first, seeded and user-written\nalike — the command twin of `GET /asterism/series-strategies`.\nRules, not groups: what a rule put on which key is a different\nquestion (see the route's doc)."
    },
    {
      "path": "asterism_ui::commands::list_sessions",
      "kind": "function",
      "docs": "Sessions view — one row per `session_id` in the query scope.\nSame `ListAssetsQuery` shape as the Messages view; the UI wires\nits view-mode toggle to swap `list_assets` ↔ `list_sessions`."
    },
    {
      "path": "asterism_ui::commands::list_settings",
      "kind": "function",
      "docs": "Every known application setting, resolved through code default →\nenvironment variable → stored row (last wins). Each row reports the\nlayer that supplied it (`source`), the whole chain it came from\n(`layers`), and the registry metadata, so the settings panel can\nrender a control and show where its value came from without a\nsecond call."
    },
    {
      "path": "asterism_ui::commands::list_shared_lines",
      "kind": "function",
      "docs": "Every line a team hosts, without its history.\n\nThe panel these go in is its own, separate from the local lines —\nwhich is what having two sources honestly looks like (decision 16)."
    },
    {
      "path": "asterism_ui::commands::list_snapshots_containing",
      "kind": "function",
      "docs": "Reverse lookup — every Snapshot whose asset_ids list contains this\nasset (P5). Renders on the detail panel as \"included in these\nfreezes\" chips."
    },
    {
      "path": "asterism_ui::commands::list_streams",
      "kind": "function",
      "docs": "The stream names [`list_observations`]'s `stream` filter accepts —\nthe command twin of `GET /asterism/observations/streams`. Published\nbecause the set is closed, and a caller guessing at it is how a\nfilter ends up silently doing nothing."
    },
    {
      "path": "asterism_ui::commands::list_tag_counts",
      "kind": "function",
      "docs": "Sidebar Tags section — every tag paired with the number of\ndistinct assets attached to it. `persona_id` restricts the count\nto that persona's assets so the sidebar tracks the active persona\nfilter; pass `None` for a global count."
    },
    {
      "path": "asterism_ui::commands::list_tag_suggestions",
      "kind": "function",
      "docs": "Lists what the bound visual model proposed for one asset (#112),\nscore-descending, rulings included. Empty without a model."
    },
    {
      "path": "asterism_ui::commands::list_team_device_tokens",
      "kind": "function",
      "docs": "The device tokens this account holds, on whatever machines.\n\nOwner-scoped by the route rather than by anything here, which the\nwire's `DeviceTokensDto` states where it is defined. Mapped to the\ncontract for the reason the roster's and the ledger's are — the\nwire is what a member's client and a team server say to each other,\nand a screen holds one vocabulary."
    },
    {
      "path": "asterism_ui::commands::list_thread_messages",
      "kind": "function",
      "docs": "Lists the Messages of a Thread."
    },
    {
      "path": "asterism_ui::commands::list_threads",
      "kind": "function",
      "docs": "Lists Threads under the given anchor, freshest first. Archived\nThreads are excluded unless `include_archived` is `true`."
    },
    {
      "path": "asterism_ui::commands::merge_assets",
      "kind": "function",
      "docs": "The manual merge verb: a person's ruling that a set of rows is one\nthing, carried out. Not scoped to the queue — the caller declares\nthe whole set (keeper + discards + the members they saw on screen)\nand the verb folds the discards into the keeper.\n\n`command.dry_run: true` returns a preview and writes nothing;\n`false` commits the fold. Both branches return the same\n[`MergeAssetsDto`] shape (a run following a preview reads the\nanswer back on it); [`MergeAssetsDto::committed`] tells them apart.\n`refusals` and `warnings` ride on the response — a refused pair is\nnot an error but a decision the caller has to re-make, and a\nwarning is what a rule was protecting before the caller confirms."
    },
    {
      "path": "asterism_ui::commands::merge_groups",
      "kind": "function",
      "docs": "Merges one manual group into another and deletes the source\n(duplicate-group consolidation). Returns the number of\nmembers moved."
    },
    {
      "path": "asterism_ui::commands::merge_tags",
      "kind": "function",
      "docs": "Folds one tag channel into another and deletes the source — the\ncommand twin of `POST /asterism/tags/merge`. `command.dry_run`\nreports the same numbers without writing; merge is not undoable."
    },
    {
      "path": "asterism_ui::commands::move_dir",
      "kind": "function",
      "docs": "Re-parents a Dir (`None` = to the root); cycle-guarded."
    },
    {
      "path": "asterism_ui::commands::move_group_to_dir",
      "kind": "function",
      "docs": "Files a Group under a Dir (`None` = back to the root)."
    },
    {
      "path": "asterism_ui::commands::my_teams",
      "kind": "function",
      "docs": "The teams this window's account belongs to.\n\nThe read a picker is over, and one a caller makes before they have\na team to name. Mapped to the contract for the reason the roster's\nand the ledger's are — the wire is what a member's client and a\nteam server say to each other, and a screen holds one vocabulary.\n\nWhat it answers, and what a screen must not conclude from an empty\nlist, is on `MyTeamsDto`."
    },
    {
      "path": "asterism_ui::commands::open_forge_line",
      "kind": "function",
      "docs": "Opens a line."
    },
    {
      "path": "asterism_ui::commands::open_forge_pursuit",
      "kind": "function",
      "docs": "Opens work against a line.\n\nThe line is named in the command rather than as an argument, because\nthis is the one verb here that does not have a pursuit to name yet.\nA caller asking \"what work is against this line\" wants\n[`list_forge_pursuits_of_line`]."
    },
    {
      "path": "asterism_ui::commands::open_forge_thread",
      "kind": "function",
      "docs": "Opens a conversation about something in the forge.\n\n**The anchor is resolved, not accepted.** The command names ids and\na kind; the service reads the pursuit or the line and the model\nbuilds the anchor from what it finds, so a pursuit nobody opened and\nan entry the round never touched are two different refusals."
    },
    {
      "path": "asterism_ui::commands::open_shared_pursuit",
      "kind": "function",
      "docs": "Opens work against a shared line.\n\nDecision 10 is why nothing is copied first: working on a shared\nline needs no clone, so this is the verb a member reaches for\nrather than the one that takes a copy home."
    },
    {
      "path": "asterism_ui::commands::organize_by_location",
      "kind": "function",
      "docs": "Backfill: auto-organises existing assets under a Dir tree derived\nfrom `source_locator` — the command twin of\n`POST /asterism/organize/by-location`. Synchronous rather than a\njob: the summary comes back when the pass finishes, and a large\nlibrary is a multi-minute wait (the service doc carries the cost)."
    },
    {
      "path": "asterism_ui::commands::paste_image_import",
      "kind": "function",
      "docs": "Writes a clipboard-pasted image blob to\n`$HOME/asterism/pasted/paste-<ts>.<ext>` and dispatches\n`add_asset` for it. The MIME-type hint picks the extension; the\ndownstream pipeline still sniffs the actual container so a\nslightly-wrong `image/webp` label does not stop cover_gen /\nthumb_gen from producing previews."
    },
    {
      "path": "asterism_ui::commands::patch_session_metadata",
      "kind": "function",
      "docs": "Partially updates a Session's metadata (`title` / `note` /\n`cover_hint`). Each `null` field is left unchanged. Use\n[`rename_session`] to clear the title back to null."
    },
    {
      "path": "asterism_ui::commands::post_asset_comment",
      "kind": "function",
      "docs": "Posts a new comment. See [`PostAssetCommentCommand`] for the\nauthor_kind / author_persona_id semantics."
    },
    {
      "path": "asterism_ui::commands::post_chapter_mark",
      "kind": "function",
      "docs": "Adds a section to a band the person owns."
    },
    {
      "path": "asterism_ui::commands::post_material_mark",
      "kind": "function",
      "docs": "Places a new mark. See [`PostMaterialMarkCommand`] for the anchor and\nauthor_kind / author_persona_id semantics."
    },
    {
      "path": "asterism_ui::commands::promote_asset_to_team",
      "kind": "function",
      "docs": "Hands one of this library's assets to a team, against open work.\n\nThe act #66 exists for, and the one entry point content has (#148\ndecision 5): the pursuit is named because a team never holds an\nasset that is not attached to work.\n\n**What travels is decision 4's and is decided below this command**,\nin `PromotedMark::gather`, which is the only thing that can say\nwhich marks qualify: the filter is over the layer a mark sits in,\nand `MaterialMarkDto` drops the `layer_id` that join is made of.\nSo the marks are read through their repository rather than their\nservice, which is the same arrangement `publish` makes one screen\nover and for the same reason.\n\n**Not safe to press twice, and the answer says so rather than the\ncommand refusing.** Decision 7 mints a `TeamAsset` per promotion.\nWhat stops a second one is a relation row this machine wrote, which\nthe client reads before anything is sent — so a repeat is answered\nfrom here with `already_promoted` and nothing crosses the wire.\nThat is a fact about this machine and not about the team: another\nmember promoting the same bytes gets their own `TeamAsset`, and\nnothing here can see it.\n\n`named` is the caller's rather than the asset's title, and\n`Promotion::named` says why."
    },
    {
      "path": "asterism_ui::commands::promote_snapshot_to_group",
      "kind": "function",
      "docs": "Promotes a Snapshot into a hand-owned Group (mirror of\n`PromoteTagToGroup`): bulk-attaches every member in frozen order\nand stamps `origin_snapshot_id` as the Group's birth record."
    },
    {
      "path": "asterism_ui::commands::promote_tag_to_group",
      "kind": "function",
      "docs": "Snapshots every asset carrying a tag into a newly-created Group.\nThe tag itself is left alone; the promotion is a one-shot copy\n(see `AssetService::promote_tag_to_group` for the rationale)."
    },
    {
      "path": "asterism_ui::commands::promote_volatile_selection",
      "kind": "function",
      "docs": "Fuses freeze + promote for the grid's volatile pick (W5-d):\nmints a Snapshot from the picked ids (content-hash deduped)\nand promotes it into a hand-owned Group in one step — the\nright-click \"Group-ify selection\" entry, no pre-existing Snapshot\nneeded."
    },
    {
      "path": "asterism_ui::commands::publish_line_to_team",
      "kind": "function",
      "docs": "Seeds a team's line from a local one (#148 decision 11).\n\n`reenact` is the option at init and nowhere else. It replays the\nchain — one pursuit and one close per change point — and it is a\n**re-enactment**: the acts are restamped to whoever published, so\nthe team's line does not record who did the work upstream. It also\nsends every content the line ever named, which is usually far more\nthan what the line holds now. The panel says both of those things\nbefore it offers the choice."
    },
    {
      "path": "asterism_ui::commands::pull_tag_head",
      "kind": "function",
      "docs": "Enqueues a `HeadPull` install of a fetched head artifact — the\ncommand twin of `POST /asterism/heads/pull` (#132 phase 3). The\ncaller fetches the artifact from the instance's registry (which\nsits behind its own session gate) and hands it over; verification\n— encoder identity, shapes, the startup-bind checks — is the\njob's, and installing promotes on the next launch."
    },
    {
      "path": "asterism_ui::commands::purge_asset",
      "kind": "function",
      "docs": "Permanently deletes an already-trashed asset. Conflicts when the\nasset is still live — trash it first."
    },
    {
      "path": "asterism_ui::commands::purge_group",
      "kind": "function",
      "docs": "Permanently deletes an already-trashed Group (cascades the m:n\nrows). Conflicts when the Group is still live."
    },
    {
      "path": "asterism_ui::commands::purge_persona",
      "kind": "function",
      "docs": "Permanently deletes an already-trashed persona and everything it\nholds. Conflicts when the persona is still live."
    },
    {
      "path": "asterism_ui::commands::push_forge_round",
      "kind": "function",
      "docs": "Writes a round.\n\nThe line is not read, which is what makes this the operation that\ncan run all day without touching anything anybody else is using.\nWhat it does check is that the content each operation names exists."
    },
    {
      "path": "asterism_ui::commands::push_shared_round",
      "kind": "function",
      "docs": "Writes a round into open work on a shared line.\n\nA round is a request rather than a landing: `push` does not read\nthe line, which is what lets two members work against one line\nwithout contending, and nothing reaches the line until a close with\n`satisfied`.\n\nThe projections a round may carry ride beside content entering the\nteam (#148 decision 12), and this surface has no verb that brings\nany in — so it passes an empty set. The two callers that fill the\nargument are the ones that do: the promotion, and publishing a line."
    },
    {
      "path": "asterism_ui::commands::random_assets",
      "kind": "function",
      "docs": "A random handful out of the current filter — the sidebar's\n\"🎲 Random\".\n\nThe grid shows the picks in place of its listing, so nothing about\nthe answer is a page: it cannot be scrolled past `k`, and asking\nagain answers differently. That is the feature.\n\n**Not exposed over MCP on purpose.** The picks exist to give a person\nsomething to look at when they do not know what they are looking for;\nan agent asking for assets under a filter wants `asset_list`, which\nenumerates, counts, and can be paged."
    },
    {
      "path": "asterism_ui::commands::rebuild_edges",
      "kind": "function",
      "docs": "Enqueues an incremental constellation-edge rebuild for the asset.\nReturns the engine's task id."
    },
    {
      "path": "asterism_ui::commands::rebuild_index",
      "kind": "function",
      "docs": "Enqueues a batch `IndexRebuild` job and returns its task id — the\ncommand twin of `POST /asterism/index/rebuild`. Idempotent on an\nalready-indexed DB; progress reaches the jobs ticker."
    },
    {
      "path": "asterism_ui::commands::rebuild_sessions",
      "kind": "function",
      "docs": "Enqueues a `SessionRebuild` job. The precomputed rkyv snapshot\nwas retired in the Session 1st-class migration, so the handler\nis a no-op today; the tauri command stays for wire compatibility\nso a future reconciliation pass has a caller-visible entry\npoint (progress emits via the shared `job:progress:{id}` channel)."
    },
    {
      "path": "asterism_ui::commands::record_diag",
      "kind": "function",
      "docs": "Appends one webview-origin diagnostic to `diag_log` — the capture\nhalf lives in `lib/diag.ts` (console/error hooks), the persistence\nhalf in the shared tracing subscriber. Fire-and-forget on the UI\nside, like `record_event`. Stateless: the re-emit helper writes\nthrough the process-global subscriber, not a service handle."
    },
    {
      "path": "asterism_ui::commands::record_event",
      "kind": "function",
      "docs": "Appends one telemetry event to the local `event_log`. Fire-and-\nforget on the UI side (a lost event is preferable to a blocked\ninteraction)."
    },
    {
      "path": "asterism_ui::commands::redispatch",
      "kind": "function",
      "docs": "Re-runs a finished dispatch with the same frozen input (P2)."
    },
    {
      "path": "asterism_ui::commands::register_persona",
      "kind": "function",
      "docs": "Registers a new persona."
    },
    {
      "path": "asterism_ui::commands::rehome_dropped_path",
      "kind": "function",
      "docs": "Rehomes a dropped path into `$HOME/asterism/dropped/`\nwhen it lives under a volatile TEMP dir (macOS screenshot\nstash lands in `/private/var/folders/…/TemporaryItems/`, and\nscreenshots dragged out of the preview thumbnail vanish once\nthe preview closes — a stale locator would then break every\ndownstream read). The copy also drops the path into the Tauri\n`assetProtocol` scope so `convertFileSrc` works for full-window\nimage playback. Returns the durable path; the frontend feeds it\nstraight into `add_asset`."
    },
    {
      "path": "asterism_ui::commands::reject_tag_suggestion",
      "kind": "function",
      "docs": "Rejects one tag suggestion (#112); this model never proposes the\npair again."
    },
    {
      "path": "asterism_ui::commands::remeasure_dims",
      "kind": "function",
      "docs": "Re-reads artefacts and rewrites `width_px` / `height_px` — the\ncommand twin of `POST /asterism/assets/remeasure`, with the same\ntwo shapes and the same refusal to accept both: `asset_ids` is \"I\nput the right file behind these, read them again\" (overwrites);\n`scope` is `unlooked` / `unmeasured` / `all`, and only `all`\nreplaces existing answers. Returns the enqueued task ids (one job\nper asset on the id shape, one batch job on the scope shape)."
    },
    {
      "path": "asterism_ui::commands::remove_asset_from_group",
      "kind": "function",
      "docs": "Idempotent remove of an asset from a Group."
    },
    {
      "path": "asterism_ui::commands::rename_dir",
      "kind": "function",
      "docs": "Renames a Dir."
    },
    {
      "path": "asterism_ui::commands::rename_forge_line",
      "kind": "function",
      "docs": "Moves the line's own description. Not a landing: nothing goes on the\nchain."
    },
    {
      "path": "asterism_ui::commands::rename_forge_thread",
      "kind": "function",
      "docs": "Names the conversation, or takes its name off.\n\nA title is a label on the conversation rather than something said in\nit, so this writes no message. `title` absent takes the name off."
    },
    {
      "path": "asterism_ui::commands::rename_group",
      "kind": "function",
      "docs": "Renames a Group."
    },
    {
      "path": "asterism_ui::commands::rename_session",
      "kind": "function",
      "docs": "Renames a Session (title-only write). Passing `title: null`\nclears the title back to the untitled state — this is the sole\npath that expresses NULL because\n[`patch_session_metadata`] treats per-field `null` as \"leave\nunchanged\"."
    },
    {
      "path": "asterism_ui::commands::rename_tag",
      "kind": "function",
      "docs": "Renames a tag channel in place — the command twin of\n`POST /asterism/tags/rename`. Rejected when the name belongs to\nanother tag: rename never merges (that is [`merge_tags`])."
    },
    {
      "path": "asterism_ui::commands::reopen_forge_line",
      "kind": "function",
      "docs": "Takes it back out."
    },
    {
      "path": "asterism_ui::commands::reorder_group_assets",
      "kind": "function",
      "docs": "Rewrites the front-to-back order of a Group's assets after a drag."
    },
    {
      "path": "asterism_ui::commands::reorder_group_children",
      "kind": "function",
      "docs": "Rewrites the order of a Group's child groups."
    },
    {
      "path": "asterism_ui::commands::reorder_personas",
      "kind": "function",
      "docs": "Rewrites `display_order` across a persona slice."
    },
    {
      "path": "asterism_ui::commands::rescan_duplicates",
      "kind": "function",
      "docs": "Re-derives duplicate conflicts from fingerprints already on the\nrows — the command twin of `POST /asterism/duplicates/rescan`.\nTakes no input: the pass is idempotent, never folds, and always\nwalks the whole library (the route's doc has the reasoning).\nReturns the enqueued job's task id."
    },
    {
      "path": "asterism_ui::commands::reset_setting",
      "kind": "function",
      "docs": "Clears one setting override and returns the value that now applies.\nIdempotent — resetting a key that was never overridden succeeds.\n\nLike [`set_setting`], the response is the *resolved* value — here,\nthe layer directly beneath the row just removed: `\"env\"` when the\nkey's variable is exported and usable, `\"default\"` otherwise.\nClearing the stored row does not clear the environment."
    },
    {
      "path": "asterism_ui::commands::resolve_duplicate_conflict",
      "kind": "function",
      "docs": "Answers one duplicate question — `folded` (queues the fold onto\n`keeper_id`, which must be one of the pair) or `kept` (both rows\nstay). Refused when the row was already answered or when either\nside has since been folded away or trashed."
    },
    {
      "path": "asterism_ui::commands::resolve_forge_pursuit",
      "kind": "function",
      "docs": "Lets the line's rule answer whatever this work collides with.\n\n**It succeeds whether or not a round was written, and the answer\nsays which.** A rule that leaves collisions to a person writes\nnothing; that is an outcome, not a failure, so `round` is absent\nrather than this being an error, and `collisions` carries what is\nleft either way."
    },
    {
      "path": "asterism_ui::commands::restore_asset",
      "kind": "function",
      "docs": "Returns a trashed asset to the live set."
    },
    {
      "path": "asterism_ui::commands::restore_group",
      "kind": "function",
      "docs": "Returns a trashed Group to the sidebar."
    },
    {
      "path": "asterism_ui::commands::restore_persona",
      "kind": "function",
      "docs": "Returns a trashed persona and the assets that went with it."
    },
    {
      "path": "asterism_ui::commands::revoke_team_device_token",
      "kind": "function",
      "docs": "Revokes one of this account's device tokens.\n\nRevoking the row this machine is holding forgets it here too, so\nthe next launch shows the password form rather than presenting a\ntoken the server has already dropped. Revoking another machine's\nrow leaves this one alone — that is the point of the listing.\n\nThe revoke itself needs no identity rule: it goes down the live\nclient, so the connection it acts on is the connection in hand by\nconstruction. The rule applies to the second half, where the file\nis consulted — a handle is unique to the server that issued it, so\nthe stored pair has to name this connection before a matching\n`token_id` means the row just dropped was the one this machine\nholds. That is\n[`stored_connection`](crate::stored_connection)'s \"which connection\na verb acts on\", third case."
    },
    {
      "path": "asterism_ui::commands::say_in_forge_thread",
      "kind": "function",
      "docs": "Says something.\n\nAnswers with the message it wrote. A reply naming a message of\nanother conversation is refused as invalid input: the caller\naddressed one conversation and named something that is not in it,\nwhich no change of state makes true."
    },
    {
      "path": "asterism_ui::commands::search_asset_ids",
      "kind": "function",
      "docs": "The same retrieval as `search_assets`, reduced to the rank order.\n\nFeeds the grid's `✦ Relevance` sort: the page itself comes from the\nexact (Query-side) read, and this only says which of those rows the\nretriever considers the better match. The ids are an ordering hint,\nnot a membership answer.\n\n**Not exposed over MCP on purpose.** An agent asking about relevance\nalready has `asset_search`, which hands back ranked cards; an\nids-only reply would only be useful to something that is already\npainting a grid it fetched by other means."
    },
    {
      "path": "asterism_ui::commands::search_assets",
      "kind": "function",
      "docs": "Full-text / fuzzy search."
    },
    {
      "path": "asterism_ui::commands::set_default_material_layer",
      "kind": "function",
      "docs": "Chooses the band the panel shows, and the one a new mark lands in.\n\nReturns nothing: the flag moves off whichever band held it, so the\ncaller re-reads the asset's bands rather than patching one entry."
    },
    {
      "path": "asterism_ui::commands::set_forge_line_strategy",
      "kind": "function",
      "docs": "Points the line at a different rule, from here on."
    },
    {
      "path": "asterism_ui::commands::set_persona_profile",
      "kind": "function",
      "docs": "Upserts the persona's identity signal."
    },
    {
      "path": "asterism_ui::commands::set_persona_theme",
      "kind": "function",
      "docs": "Sets (or clears) the wallpaper for a persona."
    },
    {
      "path": "asterism_ui::commands::set_setting",
      "kind": "function",
      "docs": "Stores one setting override and returns the value that now applies.\n\nA successful write always resolves to `source: \"stored\"` — nothing\noutranks the user's choice. The resolved row is returned rather than\nthe input so the caller also gets the refreshed `layers` chain.\nRejects a value that does not match the key's declared kind or falls\noutside its range, and an unknown key."
    },
    {
      "path": "asterism_ui::commands::shared_line_history",
      "kind": "function",
      "docs": "A shared line and its whole history."
    },
    {
      "path": "asterism_ui::commands::shared_line_pursuits",
      "kind": "function",
      "docs": "The work against a shared line, open and ended alike.\n\nNo mapping, unlike the ledger's and the roster's: the forge is the\nsame on both planes, so a shared line answers with the contract's\nown `ForgePursuitDto` and there is no second vocabulary to cross.\nThat is #148 decision 19 showing through — the mirror is path for\npath and shape for shape."
    },
    {
      "path": "asterism_ui::commands::shared_line_states",
      "kind": "function",
      "docs": "What is on a shared line, folded from its chain by the server."
    },
    {
      "path": "asterism_ui::commands::shared_pursuit",
      "kind": "function",
      "docs": "One piece of work on a shared line, as it stands."
    },
    {
      "path": "asterism_ui::commands::snapshot_members",
      "kind": "function",
      "docs": "Snapshot view members (`snapshot_members`): renderable cards\nin frozen `position` order (later-deleted assets are absent)."
    },
    {
      "path": "asterism_ui::commands::stored_team_connection",
      "kind": "function",
      "docs": "What the connect form pre-fills from, or `None` when this machine\nremembers no server.\n\nEvery field is safe to hand a window, which\n[`StoredConnection`](crate::stored_connection::StoredConnection) is\nwhere it is argued: the token has no path into that shape."
    },
    {
      "path": "asterism_ui::commands::team_ledger_page",
      "kind": "function",
      "docs": "One page of a team's ledger, seq ascending (#148 decision 18).\n\n`after` is the `next_after` of the page before, or nothing for the\nfirst. What comes back can carry a cursor even when it happened to\nend on the last event there is — [`TeamLedgerPageDto::next_after`]\nsays why, and a caller reading a null cursor as \"that was\neverything\" is claiming something this read cannot answer.\n\nThe page size is the caller's: a screen paging by hand and one\nfollowing the stream want different ones, and the server's own\nmaximum is the only ceiling either has.\n\n**This is where the two vocabularies meet.** The wire's shapes are\nwhat a member's client and a team server say to each other; the\ncontract's are what this app says to its own frontend, and\n`bindings.ts` is a projection of the contract alone. So the mapping\nhappens here rather than by handing a wire type to a screen, which\nwould give every caller of this command a second vocabulary to\nknow. `asterism-contract::teams` argues the same boundary from its\nown side, and `tests/boundary.rs` holds it."
    },
    {
      "path": "asterism_ui::commands::team_roster",
      "kind": "function",
      "docs": "Who is in a team, and in what role.\n\nIds rather than names, because that is what a membership row holds\n— see [`TeamRosterMemberDto`]. The mapping is here for the reason\nthe ledger's is: the wire is what a member's client and a team\nserver say to each other, and a screen holds the contract's\nvocabulary alone."
    },
    {
      "path": "asterism_ui::commands::team_server_session",
      "kind": "function",
      "docs": "Whether this window is talking to a team server."
    },
    {
      "path": "asterism_ui::commands::train_tag_head",
      "kind": "function",
      "docs": "Enqueues a `HeadTrain` run over the rulings under the bound\nencoder — the command twin of `POST /asterism/heads/train` (#132).\nNo input: the corpus is whatever rulings exist, so there is\nnothing to scope. Returns the job's task id; the completion\nmessage carries the held-out verdict and whether promotion\nhappened. #130's model panel is where a screen invokes this."
    },
    {
      "path": "asterism_ui::commands::trash_asset",
      "kind": "function",
      "docs": "Moves an asset to the trash (reversible)."
    },
    {
      "path": "asterism_ui::commands::trash_group",
      "kind": "function",
      "docs": "Moves a Group to the trash (reversible; membership and drag order\nsurvive, member assets are untouched)."
    },
    {
      "path": "asterism_ui::commands::trash_persona",
      "kind": "function",
      "docs": "Moves a persona and every asset it holds to the trash (reversible)."
    },
    {
      "path": "asterism_ui::commands::unlink_group",
      "kind": "function",
      "docs": "Removes a Group-in-Group connection."
    },
    {
      "path": "asterism_ui::commands::update_asset_meta",
      "kind": "function",
      "docs": "Partially updates an asset's metadata."
    },
    {
      "path": "asterism_ui::commands::update_asset_meta_batch",
      "kind": "function",
      "docs": "Partially updates metadata for multiple assets in one call."
    },
    {
      "path": "asterism_ui::commands::update_modality",
      "kind": "function",
      "docs": "Partially updates a modality master row (each omitted field is left\nunchanged). Used by drag-reorder (`sort_order`) and the settings UI."
    },
    {
      "path": "asterism_ui::commands::update_query_group_query",
      "kind": "function",
      "docs": "\"Update query\": validates + persists a replacement rule (rejecting\ndependency cycles) and re-evaluates the membership."
    },
    {
      "path": "asterism_ui::commands::update_series_strategy",
      "kind": "function",
      "docs": "Partially updates a series rule (each omitted field is left\nunchanged) — the command twin of\n`PATCH /asterism/series-strategies/{id}`. The `id` argument names\nthe target and overwrites the body's copy, the same arbitration the\nroute applies to its path segment."
    },
    {
      "path": "asterism_ui::commands::visual_model_status",
      "kind": "function",
      "docs": "Which visual model this process bound, if any (#112)."
    },
    {
      "path": "asterism_ui::error",
      "kind": "module",
      "docs": "`UiError` — error type crossed by Tauri command handlers.\n\nTauri commands must return `Result<T, E>` where `E: Serialize`\n(`anyhow` will not do). We use a `serde` tagged enum\n(`{ kind, message }`) so the TypeScript side can pattern-match on the\nvariant.\n\nInternally tagged with named fields rather than\n`content = \"message\"` around a tuple, and the reason is one field\non one variant: a conflict carries `reason` beside its message, and\na variant holding two things cannot be a tuple behind a `content`\nkey without nesting them. What crosses the wire is unchanged for\nevery other variant — `{ kind, message }`, read that way in\n`src/lib/mutate.ts` — and a conflict is that plus one field a\nreader is free to ignore."
    },
    {
      "path": "asterism_ui::error::UiError",
      "kind": "enum",
      "docs": "Error returned to the frontend from every Tauri command."
    },
    {
      "path": "asterism_ui::run",
      "kind": "function",
      "docs": "Tauri v2 app entrypoint (mobile-ready).\n\nThe setup hook initialises the whole backend (open the SQLite isle,\nrun migrations, start the job engine, wire up services), manages the\nresulting `AppState`, opens the desktop window (unless `--headless`),\nand spawns the loopback HTTP server that mirrors the Tauri command\nsurface. Command handlers stay thin — one per use-case call — and\nlive in the `commands` module."
    },
    {
      "path": "asterism_ui::state",
      "kind": "module",
      "docs": "`AppState` — service DI + backend initialisation.\n\nTauri's own `State` container gives us `Arc`-style sharing, so this\nstruct just holds `Arc<Service>` fields and does not wrap again. The\nheavy lifting is delegated to the shared `asterism_server::core_init`\n(`Full` mode: read-write tantivy index + job worker); this module only\nadapts the returned `CoreCtx` into `AppState` and supplies the Tauri\nprogress emitter."
    },
    {
      "path": "asterism_ui::state::AppState",
      "kind": "struct",
      "docs": "Bundle of services registered as Tauri state.\n\nNote: no isle handle here. The precomputed session snapshot is\nrebuilt through `AssetService::rebuild_sessions` (which enqueues\na `SessionRebuild` job); the store's SQLite handle lives inside\nthe store itself."
    },
    {
      "path": "asterism_ui::state::TeamsConnection",
      "kind": "struct",
      "docs": "A live team session, and the pair that names it.\n\nThe client alone cannot answer \"which connection is this\" the way\nthe stored state asks it: `stored_connection` keys an entry by\nserver **and login**, and a session knows the server it was built\nwith and the `user_id` the server answered with — not the login as\nsomebody typed it. So the pair travels beside the session, set once\nwhere the connection is made and read by every verb that has to\ndecide whether what this machine remembers is what it is holding.\n\nThat rule, and what goes wrong without it, is on\n[`stored_connection`](crate::stored_connection)'s \"which connection\na verb acts on\"."
    },
    {
      "path": "asterism_ui::state::init",
      "kind": "function",
      "docs": "Initialises the whole backend and returns both the Tauri `AppState`\nand the HTTP [`ServerCtx`]. Invoked from the Tauri setup hook. Opens\nthe shared core in `Full` mode (this process is the single tantivy\nwriter and runs the job worker).\n\nBoth context structs are built from the same `CoreCtx` so the UI and\nthe loopback HTTP surface it serves share one service graph and one\njob worker. Neither wrapper constructs a service of its own: one\nbuilt here rather than in `init_core` would be reachable from this\ntransport only, which is how the comment thread spent four commands\nwith no HTTP route. All fields are `Arc`/`Clone`, so the bundle is\nassembled by cloning the handles before `AppState` takes ownership of\nthe originals."
    },
    {
      "path": "asterism_ui::stored_connection",
      "kind": "module",
      "docs": "What this machine remembers about a team server between windows\n(#204).\n\n# The invariant\n\n**The disk never holds a primary credential.** Not the password,\nnot anything a verifier issued. The one credential this machine may\nkeep is a device token the server minted — expiring, listable and\nrevocable — and it lives in the OS keychain, never in a file.\nEverything else worth remembering is not a credential at all, and\nthat half lives in the profile home as ordinary JSON.\n\nTwo stores, and the split is the rule rather than a convenience:\nanything reaching [`write_metadata`] is by construction something a\nreader of the file may see, and the only value that does not go\nthrough it is the token, which has no path to a file from anywhere\nin this module.\n\n# Why the pair is keyed by server and login\n\nOne stored connection per server URL and login is the shape the\nissue settles on, so [`account_key`] is the whole identity of an\nentry: presenting a token minted for one account to another server\nis not a case this module can reach, because the key that found the\ntoken names both.\n\n# Which connection a verb acts on\n\n**A verb touches the stored state only when the stored metadata\nnames the connection in hand** — same server, same login, compared\nby [`StoredConnection::names`]. The two stores answer different\nquestions and only one of them is about the window: the file says\nwhat this machine was last told to remember, and the session says\nwhat it is talking to now. They agree on the ordinary path and come\napart on a real one — a remembered server that was down when the\npanel opened leaves its metadata in place, the connect form is\neditable over the values it pre-filled, and the next thing signed\nin to is somebody else.\n\nWithout the rule, a verb reading the file alone acts on the wrong\nconnection twice over: it sends one server's revocation handle to\nanother, where it is an idempotent `204` that deletes nothing, and\nit drops the first server's entry while the row it names lives out\nits ninety days. Signing out would then revoke nothing and forget\neverything. So the file is a claim to be checked rather than an\ninstruction to be followed, and a verb that cannot match it does\nthe half it is sure of — end the session — and leaves the rest\nalone.\n\n[`remember_this_device`](crate::commands) and\n[`disconnect_team_server`](crate::commands) are where this is\napplied, and `revoke_team_device_token` is the third: it revokes on\nthe live client, which is the connection in hand by construction,\nand consults the file only to decide whether the row it just\ndropped was this machine's own.\n\n# What a remember retires, and in what order\n\n**The file is the sole index of the keychain.** Nothing here\nenumerates entries, and nothing else in the app holds a list of\nthem, so an entry the file does not name is reachable from neither\nstore — the \"credential nothing knows how to revoke\" [`forget`]\nexists to prevent, arrived at by a third door. That makes the order\nof a remember's writes part of the invariant rather than a detail\nof one function: the displaced entry goes first, the file second,\nand the entry the file has just named last.\n\nA crash inside that order leaves one of two states, and which one\nturns on whether the pair being written is the pair being\ndisplaced. A **new pair** lands in the state the next launch\nalready repairs — a file naming a pair whose entry is missing,\nwhich `connect_team_server_stored` answers by dropping the metadata\nand showing the password form.\n\n**The same pair remembered twice is the exception**, because\n[`retire_replaced`] returns early there by design: nothing takes\nthe entry away, so a run that dies between the file and the\nkeychain leaves the entry holding the *previous* token while the\nfile's `token_id` names the row just minted. Nothing is orphaned —\nthe entry is the one the file names — and what it leaves is a\nhandle that is ahead of the credential behind it. The next launch\nreconnects on the old token and works; a disconnect after it\nrevokes the row the file names, which is the new one, and the old\nrow — whose token this machine was actually presenting — lives out\nits fixed expiry with its handle written down nowhere. That is a\nrow in the owner's listing rather than a credential on this\nmachine, it is bounded by an expiry nobody has to act for, and the\nnext remember of that pair overwrites both halves. Cheaper than the\nalternative, which is retiring an entry the write is about to\nreplace and losing a working credential to the same crash.\n\nAn entry nothing names cannot be created at all, because the\nkeychain is written only after the file says what is about to go\ninto it.\n\nThe order used to be the other one: the token reached the keychain\nbefore anything reached the file, so that no metadata could name a\ntoken nothing held. That was true of the pair being written and\nsilent about the pair being displaced, where the file went on\nnaming the previous connection and the next launch reconnected to\nit — leaving the entry just written where nothing would ever look\nagain. The two costs are not alike: a missing entry is repaired on\nthe next launch by a person typing a password once more, and an\nentry nothing names is repaired by nothing.\n\n# The row behind a displaced pair\n\nRetiring an entry takes away this machine's copy of a credential\nand says nothing to the server that minted it, so the\n`device_token` row stays live under the same [`device_label`] as\nthe one that replaced it. Two cases, and what separates them is\nwhether the session in hand can reach the row:\n\n- **The same pair remembered again.** The live session is that\n  server's, so the displaced handle — read out of the file before\n  it is overwritten, which is what [`superseded_row`] answers — can\n  be revoked over it. Best effort: a remember that stored the\n  credential did what was asked, and a revoke that failed must not\n  turn it into a refusal. What that costs is the old row left to\n  its expiry, in a listing where it can still be picked out.\n- **A different server remembered.** **Known limitation:** that row\n  cannot be revoked from here. The handle names a row on a server\n  this window is not talking to, and the session in hand belongs to\n  the new one. The row lives out its fixed expiry, listed where it\n  was issued and revocable from any window signed in there. The\n  keychain entry is retired either way, so what is left behind is a\n  row in somebody's listing rather than a credential on this\n  machine.\n\nA crashed remember leaves an unrevoked row of its own, too — the\nmint lands and the writes do not. It ends up where the other two\ndo: on the server, in the owner's listing, which is the whole\nreason that listing is a screen.\n\n# Failing quietly on the way in, loudly on the way out\n\nEvery read degrades to \"nothing is stored\" **in what it puts in\nfront of a person**. A locked keychain, a denied prompt, a profile\nhome somebody moved and a file half a disk wrote all end at the\npassword form, because a reader who is told their keychain is\nunavailable can do nothing the form does not already ask of them.\n\nWhat a read may not degrade is the answer it gives this module. The\nfile is the sole index of the keychain, so a caller deciding\nwhether to drop the file needs the one distinction the person does\nnot: an entry that is gone says the file is stale, and a keychain\nthat would not answer says nothing at all. [`read_token`] therefore\nanswers a [`StoredToken`] rather than an `Option`. Folding those\ntwo together is how one denied prompt at launch deletes the index\nof an entry that is still sitting there — the state the section\nabove says a remember cannot create, reached by a deletion instead.\n\nA write is the opposite. Somebody ticked a box asking to be\nremembered, and a keychain that refuses has to say so: the\nalternative is an app that silently forgets and asks again next\nweek, with no way to tell that from a revoked token."
    },
    {
      "path": "asterism_ui::stored_connection::StoredConnection",
      "kind": "struct",
      "docs": "What this machine remembers about a team server, none of which\nauthenticates anybody.\n\nEvery field here is safe in a file, and the module header says why\nthat is a property of the type rather than of its current fields:\nthe token has no path into this struct."
    },
    {
      "path": "asterism_ui::stored_connection::StoredToken",
      "kind": "enum",
      "docs": "What the keychain said when asked for one pair's token.\n\nThree answers rather than two, and the third is the module header's\n\"what a read may not degrade\": both of the ways there is no token\nin hand end at the password form, but only one of them is news\nabout the file. A caller that treats them alike drops the index of\nan entry that is still there the first time somebody dismisses a\nkeychain prompt."
    },
    {
      "path": "asterism_ui::stored_connection::account_key",
      "kind": "function",
      "docs": "The keychain account name for one server-and-login pair.\n\nWritten to be read: macOS shows this string in Keychain Access, so\na person auditing what an app has stored sees which account on\nwhich server rather than a hash.\n\nTrailing slashes come off so that a server typed with one and a\nserver typed without reach the same entry. Nothing else is\nnormalised — a login's case is the server's business, and folding\nit here would be this side deciding two accounts are one."
    },
    {
      "path": "asterism_ui::stored_connection::delete_metadata",
      "kind": "function",
      "docs": "Removes the non-secret half. Best effort, for [`delete_token`]'s\nreason."
    },
    {
      "path": "asterism_ui::stored_connection::delete_token",
      "kind": "function",
      "docs": "Takes the device token out of the keychain.\n\nSays nothing about whether there was one. Forgetting is reached\nfrom a logout and from a token the server rejected, and neither has\nanything to do differently on learning the entry was already gone."
    },
    {
      "path": "asterism_ui::stored_connection::device_label",
      "kind": "function",
      "docs": "What this device would call itself when asking for a token.\n\nIt names the app and the platform, not the machine: reaching a\nhostname from a bundled desktop app costs a dependency, and this\nstring's job is to let somebody reading their own listing decide\nwhat to revoke. Two machines of the same kind therefore carry the\nsame label, and the mint time beside them in the listing is what\ntells those two rows apart."
    },
    {
      "path": "asterism_ui::stored_connection::forget",
      "kind": "function",
      "docs": "Drops both halves for one server and login.\n\nThe pair is what a stored connection is, so the two are forgotten\ntogether — a keychain entry outliving its metadata is a credential\nnothing knows how to revoke, and metadata outliving its entry is a\nform that pre-fills into a reconnect that cannot work."
    },
    {
      "path": "asterism_ui::stored_connection::read_metadata",
      "kind": "function",
      "docs": "What was stored about the last server this machine was told to\nremember, or `None`.\n\n`None` covers an absent file, an unreadable one and one holding\nsomething this type does not parse — a shape that changed under a\nfile nobody migrates is a connection to forget, not a startup to\nfail."
    },
    {
      "path": "asterism_ui::stored_connection::read_token",
      "kind": "function",
      "docs": "The device token stored for this server and login.\n\nThe keychain call runs on a blocking thread because it is one: the\nfirst read of an entry can put a prompt in front of the person and\nwait for them, and an async runtime thread is not somewhere to do\nthat. A thread that does not come back at all is [`StoredToken::Unavailable`]\nfor the same reason a locked keychain is — it is a fact about this\nread rather than about the entry."
    },
    {
      "path": "asterism_ui::stored_connection::retire_replaced",
      "kind": "function",
      "docs": "Takes away the keychain entry of the pair a newly remembered one\nhas just displaced, if it displaced any.\n\nThe file holds one connection and the keychain holds an entry per\npair, so remembering a second server — or the same server under a\nsecond login, which [`account_key`] does not fold — retires a\nmetadata row and would leave an entry behind it. That entry is the\n\"credential nothing knows how to revoke\" [`forget`] exists to\nprevent, arrived at by the other door.\n\nRuns **before** the new metadata lands, which is the module\nheader's \"what a remember retires, and in what order\": the file is\nthe index of the keychain, so no entry may outlive the file's\nmention of it. A run that dies between this and the write leaves\nmetadata describing a token nothing holds, and the next launch\ndrops that metadata and asks for a password — the cost the header\naccepts, and the credential thrown away is the one the person has\njust asked to replace.\n\nTakes `previous` as an argument rather than reading the file,\nbecause the caller has to read it before overwriting it in any\ncase, and by reference because it is needed again afterwards:\n[`superseded_row`] is the other half of a displacement, and what it\nanswers cannot be recovered once the file is rewritten.\n\nReturns having done nothing when the displaced pair is the pair\nbeing written. That entry is not orphaned by the write, it is\noverwritten by it, and deleting it here would take away the entry\nthe file is about to name. The server row that displacement leaves\nbehind is [`superseded_row`]'s.\n\nBest effort, for [`delete_token`]'s reason."
    },
    {
      "path": "asterism_ui::stored_connection::superseded_row",
      "kind": "function",
      "docs": "The handle of the `device_token` row a fresh mint for this pair\ndisplaces on the server, or `None` when this displacement leaves no\nrow anything here can revoke.\n\nThe other half of [`retire_replaced`], and the two are exclusive by\nconstruction — which is the point of reading them together. A\ndisplaced pair is either the pair being written, where the keychain\nentry is overwritten and the row is what is left over, or a\ndifferent one, where the entry is retired above and the row sits on\na server the session in hand is not talking to. So this answers\n`Some` in exactly the case `retire_replaced` returns early in, and\nthe module header's \"the row behind a displaced pair\" says what the\ncaller does with each.\n\nAnswered here rather than at the call site because it is the same\nquestion [`StoredConnection::names`] asks everywhere else, and a\nsecond comparison written by hand is how the two come to disagree."
    },
    {
      "path": "asterism_ui::stored_connection::write_metadata",
      "kind": "function",
      "docs": "Writes the non-secret half, replacing what was there.\n\nOne connection, which is the v1 shape: a second server remembered\nis this file rewritten, and the entry the previous one held has\nbeen taken away by [`retire_replaced`] before this lands — the file\nindexes the keychain, so it never names one pair while another's\nentry sits outside it."
    },
    {
      "path": "asterism_ui::stored_connection::write_token",
      "kind": "function",
      "docs": "Puts a device token in the keychain, replacing whatever this\nserver and login had there.\n\nRefusals reach the caller, for the reason the module header gives:\nthis is somebody's request to be remembered, and one that did not\nhappen has to be visible."
    }
  ]
}
