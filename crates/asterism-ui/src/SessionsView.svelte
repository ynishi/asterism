<!--
  SessionsView.svelte — RETIRED (2026-07-25).

  The dedicated Sessions view was removed: Session tiles now surface
  inside the Messages grid whenever the active modality is
  "dialogue", with a "Show messages" toggle in App.svelte to
  interleave per-message tiles. Tile DOM + CSS were extracted into
  `./SessionTile.svelte` for reuse from that inline path.

  This file is retained as an empty placeholder because the current
  Svelte source tree has no in-repo delete surface; App.svelte no
  longer imports it. Follow-up removal can be done in a Human-led
  `git rm` sweep at any convenient moment.
-->
