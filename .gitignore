# Rust build artifact
target
.ryo

# Local scratch: task workspace, git worktrees
workspace
.worktrees

# Agent configuration is committed (.claude/CLAUDE.md, agents/,
# settings.json); only per-person local state stays out.
.claude/settings.local.json
.claude/CLAUDE.local.md

# OS / editor
.DS_Store
*.swp
.idea/
.vscode/
.metadata_cache/

# Tauri (asterism-ui) generated
crates/asterism-ui/dist/
crates/asterism-ui/gen/
.mcp.json

# Coexisting AI CLI scratch (codex CLI local state / AGENTS.md sidecar)
.codex/
AGENTS.md

logs

